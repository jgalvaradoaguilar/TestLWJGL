package identity;

/**
 * The only purpose of the interface is to provide an <code>Identity</code>.
 */
public interface Identifiable
{
    /**
     * Retrieve the identity.
     */
    Identity getIdentity();
}
